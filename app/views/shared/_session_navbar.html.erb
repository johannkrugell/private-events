<nav class="">
  <div class="p-3  h-16 grid grid-cols-3 gap-4 bg-indigo-50 shadow-md items-center">
    <div class="font-semibold col-span-2 text-xl text-indigo-900">
      brite events<span class="text-black text-xl">.</span> 
    </div>
    <div class="grid grid-flow-col col-span-1 gap-4 justify-items-end items-center w-max">
      <!-- If the User is logged in display the user name -->
      <% if user_signed_in? %>
        <div class="text-indigo-800 font-semibold text-sm"><%= "#{current_user.first_name} #{current_user.last_name} |" %></div>
      <% end %>
      <% if user_signed_in? %>
        <div class="text-slate-600 text-sm hover:bg-indigo-100 hover:rounded-lg hover:p-1"><%= link_to "Create Event", new_event_path %></div>
        <div class="text-slate-600 text-sm hover:bg-indigo-100 hover:rounded-lg hover:p-1"><%= link_to "My Events", my_events_path(current_user.id) %></div>
        <div class="text-slate-600 text-sm hover:bg-indigo-100 hover:rounded-lg hover:p-1"><%= link_to "All Events", events_path %></div>
      <% end %>
      <% if user_signed_in? %>
        <div class="text-slate-600 text-sm hover:bg-indigo-100 hover:rounded-lg hover:p-1"><%= button_to "Log Out", destroy_user_session_path, method: :delete %></div>
      <% else %>
        <div class="text-slate-600 text-sm hover:bg-indigo-100 hover:rounded-lg hover:p-1"><%= link_to "Log In", user_session_path %></div>
      <% end %>
    </div>
  </div>